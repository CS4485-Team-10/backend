line-length = 88
target-version = "py311"
extend-exclude = ["alembic/versions", "pipelines", "scripts"]

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["B008"]

[lint.isort]
known-first-party = ["app"]
